import { site } from "@/lib/site-data";
export default function Resume(){return <main className="shell"><section className="section"><div className="command">$ history --work</div><h1>Resume</h1><p className="section-lead">Senior DevOps & Cloud Infrastructure Engineer focused on AWS, Kubernetes, GitOps, infrastructure as code, CI/CD, observability, and secure delivery.</p><div className="timeline">{site.experience.map(x=><div className="timeline-item" key={x.company}><div className="timeline-marker"/><div><div className="timeline-head"><h3>{x.role} · <a href={x.url} target="_blank" rel="noreferrer">{x.company}</a></h3><span>{x.date}</span></div><ul>{x.bullets.map(b=><li key={b}>{b}</li>)}</ul></div></div>)}</div></section><section className="section"><div className="command">$ cat ./education</div><h2>Education</h2><div className="edu-grid">{site.education.map(e=><div className="terminal-card" key={e.degree}><h3>{e.degree}</h3><a href={e.url} target="_blank" rel="noreferrer">{e.school}</a><p>{e.date} · GPA {e.gpa}</p></div>)}</div></section></main>}
